import React from 'react';

const Footer = () => {
    // This is not HTML, this is JSX
    return (
        <div className="Footer">
          <p>That's footer</p>
        </div>
    );
}

export default Footer;